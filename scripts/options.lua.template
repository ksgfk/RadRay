radray_toolchain = {
	-- toolchain = "clang-cl"

	-- toolchain = "llvm",
	-- sdk = "/usr/local/opt/llvm",
	-- ldflags = "-L/usr/local/opt/llvm/lib/ -L/usr/local/opt/llvm/lib/c++ -Wl -rpath /usr/local/opt/llvm/lib/c++"
}
function get_options()
	return {
		build_test = true,
		enable_d3d12 = true,
		enable_metal = true,
		enable_mimalloc = true,
		enable_shader_compiler = true
	}
end
