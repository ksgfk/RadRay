radray_toolchain = {
	-- toolchain = "clang-cl"

	-- toolchain = "llvm",

	-- toolchain = "llvm-macos-brew",
}
function get_options()
	return {
		build_test = true,
		enable_d3d12 = true,
		enable_metal = true,
		enable_mimalloc = true,
		enable_dxc = true
	}
end
