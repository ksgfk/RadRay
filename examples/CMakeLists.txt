if (RADRAY_ENABLE_VULKAN)
    add_subdirectory(hello_world_vk)
endif()
if (RADRAY_ENABLE_D3D12)
    add_subdirectory(hello_world_d3d12)
endif()
if (RADRAY_ENABLE_IMGUI)
    add_subdirectory(hello_imgui)
    add_subdirectory(hello_pbr)
endif()
